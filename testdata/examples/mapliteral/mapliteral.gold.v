(* autogenerated from github.com/goose-lang/goose/testdata/examples/mapliteral *)
From New.golang Require Import defn.

Module example.
Section code.
Context `{ffi_syntax}.


(* go: mapliteral.go:3:6 *)
Definition mapliteral : val :=
  rec: "mapliteral" <> :=
    exception_do (return: ((let: "$v0" := #(W64 2) in
     let: "$k0" := #(W64 1) in
     map.literal uint64T [("$k0", "$v0")]))).

Definition pkg_name' : go_string := "github.com/goose-lang/goose/testdata/examples/mapliteral".

Definition vars' : list (go_string * go_type) := [].

Definition functions' : list (go_string * val) := [("mapliteral"%go, mapliteral)].

Definition msets' : list (go_string * (list (go_string * val))) := [].

Definition info' : pkg_info.t := {|
             pkg_info.vars := vars';
             pkg_info.functions := functions';
             pkg_info.msets := msets';
             pkg_info.imported_pkgs := [];
           |}.

#[global] Instance  : PkgInfo pkg_name' info' :=
  {}.

Definition initialize' : val :=
  rec: "initialize'" <> :=
    globals.package_init pkg_name' vars' functions' msets' (λ: <>,
      exception_do (do:  #())
      ).

End code.
End example.
